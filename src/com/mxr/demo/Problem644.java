package com.mxr.demo;

public class Problem644 {
    public double findMaxAverage(int[] nums, int k) {
        double result = -10001;
        int sum = 0, from = 0, n = nums.length;;
        for (int to = 0; to < n; ++to) {
            sum += nums[to];

        }
        return result;
    }
}
