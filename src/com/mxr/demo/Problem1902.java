package com.mxr.demo;

import java.util.Stack;

public class Problem1902 {
    public int maxDepthBST(int[] order) {
        Stack<Integer> stack = new Stack<>();
        int depth = 0, result = 0;
        for (int num : order) {
            //while (!stack.isEmpty() && num)
        }
        return result;
    }
}
